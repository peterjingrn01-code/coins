Bind KV as DB. Add secret OWNER_SETUP_KEY. Optional: RESEND_API_KEY, RESET_FROM_EMAIL. For testing reset codes set RESET_CODE_TEST_MODE=true. This clean rebuild uses JSLCoin_STATE_V6 and does not import the old broken ledger.
