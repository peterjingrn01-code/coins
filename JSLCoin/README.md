# JSL-ian Dashboard + Owner Console v1.0.2

Upload these files to the existing `coins` repo root:

- userpage.html
- ownerpage.html

Do not replace working index.html, coin.html, or worker.js yet.

## Flow
- Public/working pages remain unchanged.
- After sign in, connect to `userpage.html`.
- Owner tools live in `ownerpage.html`.

## Important
GENESIS endpoint must be added to Worker before ownerpage can initialize officially.
